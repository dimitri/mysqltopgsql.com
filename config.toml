baseURL = "https://mysqltopgsql.com/"
languageCode = "en-us"
title = "MySQL to PostgreSQL"

publishDir = "docs"

# Vendored 1:1 from ~/dev/TAOP/taop.xyz/themes/taop — real header/footer/
# typography/button/section markup and the real style.css, not a hand-ported
# approximation. This site's own layouts/ override only what's genuinely new
# (course page, FAQ hub/lesson, support page, login form) — everything else
# falls through to the real theme.
theme = "taop"

MetaDataFormat = "yaml"
DefaultContentLanguage = "en"
SectionPagesMenu = "main"
Paginate = 12
googleAnalytics = "UA-47059482-4"
enableRobotsTXT = false

[params]
  description = "Migrate from MySQL to PostgreSQL"
  github = "https://github.com/dimitri/pgloader"
  custom_css = ["css/custom.css"]
  course_url = "https://theartofpostgresql.com/from-mysql"

  # Migration to PostgreSQL — the shipped course. Structure and prices are
  # the final ones: 16 modules in three groups (6 common, 5 MySQL, 5 SQL
  # Server), and the two engine lanes are siblings, not steps — a MySQL
  # buyer never sees the SQL Server modules and vice versa. Each single-
  # engine tier is therefore 11 modules (6 common + 5 engine), which is
  # the number the checkout shows.
  #
  # One checkout holds every tier; the reader picks there. Everything on
  # the site renders from these params, so a price change is a one-line
  # edit here.
  course_checkout_url = "https://sales.theartofpostgresql.com/migrating-to-postgresql-courses/"
  course_price = "$79"                  # either single-engine lane
  course_price_complete = "$129"        # both lanes, plus Oracle when it ships
  course_price_regional = "$49"         # lower-income regions, all engines
  course_price_supporter = "$229"       # same course, higher contribution
  course_modules_total = "16"
  course_modules_lane = "11"
  course_modules_free = "4"
  # Kept for templates that still reference it; the checkout is the one
  # destination now that a real product exists.
  course_buy_url = "https://sales.theartofpostgresql.com/migrating-to-postgresql-courses/"
  twitter = "tapoueh"
  # The parent brand's own products — footer "Elsewhere" directory and the
  # About page's book mention. book_update_url is the 2026 Second Edition
  # update specifically (new PG12-18 coverage, pgvector chapter, Citus,
  # companion app.theartofpostgresql.com platform) — link that, not just
  # the book's own page, so existing-owner readers see what's new.
  book_url = "https://theartofpostgresql.com/book/"
  book_update_url = "https://theartofpostgresql.com/book/updates/"
  taop_course_url = "https://theartofpostgresql.com/course/"
  masterclass_url = "https://theartofpostgresql.com/masterclass/"

[author]
  twitter = "tapoueh"

[markup.goldmark.renderer]
  # Trusted single-author content only — needed so content/project/_index.md
  # can wrap its steps in real <section class="section"> bands (matching the
  # rest of the site's marketing-section language) instead of one long
  # markdown wall of H1s.
  unsafe = true